
const Total = ({ parts, sum }) => {
    {
        parts.map((part) =>
            sum = sum + (part.exercises)
        )
    }
    return (
        <div>
            <p><b>Number of exercises {sum}</b></p>
        </div>)
}

export default Total;